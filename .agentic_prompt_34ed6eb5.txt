% You are an expert software engineer. Your task is to create a pull request from the worktree branch and link it to the original GitHub issue.

% Context

You are working on step 13 of 13 in an agentic change workflow. This is the final step. All prompt changes have been made and verified. Now you create a PR for review.

% Inputs

- GitHub Issue URL: https://github.com/aeril716/live-answer-card-test/issues/16
- Repository: aeril716/live-answer-card-test
- Issue Number: 16
- Issue Title: [Lane 2] audio.get_utterance() — mock version
- Worktree Path: /tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16
- Artifacts Directory: /tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16/.pdd/change/issue-16
- Branch Name: change/issue-16
- Base Branch: main
- Files Changed: README.md, docs/writing-pdd-prompts.md, prompts/audio_python.prompt
- Sync Order Script: /tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/sync_order_change.sh
- Sync Order Commands: pdd sync audio
- Clean Restart Mode: false

% Issue Content
<issue_content>
Title: [Lane 2] audio.get_utterance() — mock version

Description:
## Goal

`audio.py` exposes `get_utterance()` returning one utterance at a time. Mock
only — real ElevenLabs streaming comes in a later issue. This lets the whole
team run the loop without a microphone.

## Contract — frozen

```python
def get_utterance() -> dict:
    return {{"text": "Are you guys SOC 2 certified?",
            "speaker": "prospect",
            "ts": 252.4}}

EMPTY = {{"text": "", "speaker": "unknown", "ts": 0.0}}
```

## Acceptance criteria

1. Given `USE_MOCK = True` at the top of the file, when `get_utterance()` is
   called, then it replays a hardcoded list and makes no network call.
2. The hardcoded list contains, in this order: a technical question about SOC 2,
   a follow-up asking Type I or Type II, a small-talk line about the weekend,
   and a pricing question. This is the demo sequence.
3. Given repeated calls, then each utterance is returned exactly once, in order.
4. Given the list is exhausted, then EMPTY is returned on every later call.
5. Given the function is called, then it returns immediately and never blocks.
6. Given any internal error, then EMPTY is returned and nothing is raised.
7. `ts` increases across the list.
8. The module prints one line per call in the form `[audio] out=<text>`.

## Must not

- Must not raise under any circumstance.
- Must not block the calling thread.
- Must not return the same utterance twice.
- Must not import websockets, requests, or any audio library in this issue.

## Validation

- Run `python audio.py`. A `__main__` block calls `get_utterance()` six times
  and prints each result, so exhaustion is visible.

## Done when

- Four utterances come out in order, then EMPTY forever
- `python audio.py` runs clean with no microphone and no network


Comments:

--- Comment by prompt-driven-github[bot] ---
🚀 **Job Queued!**

Job ID: `pwlYgJtJT7l3U1OEcun5`
Triggered by: @sohyunerinyang
Label: `pdd-change`

[View Live Progress](https://promptdriven.ai/jobs/pwlYgJtJT7l3U1OEcun5)

Budget cap: none

You can add or change a cap by commenting:
```text
/strength 0.7
/pdd budget N
/pdd settings
/pdd stop
```
_Run controls require write access; `/pdd settings` is read-only._

</issue_content>

% Change Summary (from previous steps)
<step8_output>
<step_report>
## Step 8: Prompt Change Analysis

**Status:** Changes Analyzed

### Summary
Create one concise prompt-native dev unit for the standalone audio mock. The new prompt will include the shared project preamble, freeze the public contract, specify the finite ordered replay and failure behavior as testable outcomes, and leave implementation structure to generation; Step 9 must also apply the two documentation updates approved in Step 5.

### Prompt Modifications

#### `prompts/audio_python.prompt`
**Current state:** The prompt does not exist. The repository has no prompt-backed audio module, while `context/project_preamble.prompt` already declares `audio.get_utterance() -> dict` as a frozen lane interface and supplies project-wide mock, logging, and failure-containment rules.

**New prompt to create:**
- Role: Create root-level `audio.py` as a synchronous, process-local, mock-only source that returns one demo utterance per call without microphone or network access. Real ElevenLabs streaming remains out of scope.
- Architecture metadata: Add a concise `<pdd-reason>` and a module `<pdd-interface>` for `get_utterance()`; add no `<pdd-dependency>` because the module has no project runtime dependency.
- Requirements:
  1. Preserve `get_utterance() -> dict`, the exact utterance keys and value types, and `EMPTY = {"text": "", "speaker": "unknown", "ts": 0.0}`; the first result is exactly `{"text": "Are you guys SOC 2 certified?", "speaker": "prospect", "ts": 252.4}`.
  2. With `USE_MOCK = True`, return a deterministic four-item demo sequence with no microphone, audio-library, or network access: the frozen SOC 2 question, a Type I/Type II follow-up, weekend small talk, and a pricing question, in that order.
  3. Return every demo utterance exactly once per process in repeated single-threaded calls, with strictly increasing fixed timestamps and `speaker="prospect"` for all four.
  4. After the fourth item, return a fresh EMPTY-shaped dictionary on every later call; when `USE_MOCK = False` in this mock-only version, also return EMPTY.
  5. Return immediately on every call. The module uses no waiting, sleeping, asynchronous work, device I/O, or network I/O.
  6. Return defensive dictionaries so caller mutation cannot change the canonical demo sequence, future results, or the exported EMPTY value.
  7. Contain ordinary internal runtime failures and return a fresh EMPTY-shaped dictionary without exposing an exception; interpreter termination signals are outside the internal-error contract.
  8. Emit exactly one line per invocation in the form `[audio] out=<text>`, including an empty `<text>` after exhaustion or fallback, while keeping user-visible text on one safe line.
  9. Include a guarded `__main__` demonstration that calls `get_utterance()` six times and prints each returned dictionary, showing four ordered utterances followed by two EMPTY results.
- Constraints: Do not import `websockets`, `requests`, or any audio library; do not block; do not replay a consumed utterance; produce no output merely by importing the module.
- Dependencies: `<include>context/project_preamble.prompt</include>` only.
- Deliverables named by the prompt: `audio.py`, `context/audio_example.py`, and `tests/test_audio.py`.

**Extension Analysis (REQUIRED):**
- Modules considered: `retrieval`, `trigger`, `screen`, `model_client`, and `band`.
- Why not extendable: `retrieval` turns questions into answer cards; `trigger` classifies utterances and suppresses repeats; `screen` renders cards; `model_client` owns provider calls; and `band` publishes optional audit records. Adding utterance acquisition or finite replay state to any of them would mix unrelated responsibilities and alter a frozen lane boundary. None exposes or owns `audio.get_utterance()`.
- Capability gap: The repository lacks the independent, process-local utterance source required to feed the existing downstream call-loop contracts without a microphone or network.

### Documentation Changes Carried Forward
- Update `README.md` with the approved `Audio mock` section describing the four-item sequence, permanent EMPTY exhaustion, per-call log, offline behavior, and `python audio.py` demonstration.
- Add `audio_python.prompt          # Lane 2, mock utterance source` to the `Repo layout` inventory in `docs/writing-pdd-prompts.md`.
- No associated documentation is reachable through the new prompt's include graph; `context/project_preamble.prompt` is shared prompt context and needs no change.

### Interface Changes
- Add the already-documented frozen public interface `audio.get_utterance() -> dict`; no existing interface changes.
- No existing prompt or generated module depends on `audio`, so this is additive and does not break current callers or tests.
- The new generated example and tests will be produced by `pdd sync`; they must not be edited directly in Step 9.

### Conflict Check
- The mock-only behavior is compatible with the preamble's frozen return shape, `USE_MOCK` convention, direct in-process calls, and fail-closed rules.
- The later live ElevenLabs plan is intentionally not included; it can replace the mock behind the same frozen interface in a future change.
- The preamble's broad “never raises” rule is interpreted consistently with Step 4: ordinary internal failures are contained, while `KeyboardInterrupt` and `SystemExit` remain process-control signals.
- Exactly-once replay is scoped to repeated single-threaded calls. Concurrent consumption guarantees are not added by this issue.

### Risk Assessment
- **Low**: The feature is an isolated, deterministic, dependency-free module with an additive frozen interface and no integration registration.
- Mitigation: State exact first-record content, ordered topics, strict timestamp increase, mutation isolation, permanent exhaustion, false-mode fallback, per-call logging, banned imports, and six-call validation as observable requirements. Regenerate with `pdd sync` and verify offline tests plus `python audio.py`.

---
*Proceeding to Step 9: Implement Changes*
</step_report>
</step8_output>

% Manual Review Required (from orchestrator aggregation)

The orchestrator collected every ``MANUAL_REVIEW: <path> — <reason>`` line emitted by Step 9 JSON/prose, synthesized from Step 10 associated-doc conflicts, or emitted by Step 11/12 review-loop JSON/prose. These are items the workflow could not safely auto-resolve. They MUST be surfaced in the PR body so reviewers can address them — not buried elsewhere on the issue thread.

<manual_review_lines>
None
</manual_review_lines>

% User Story Coverage (from issue-derived story generation)

The orchestrator generated an issue-derived user story for the existing,
non-runtime prompts changed by this workflow and staged it for this PR. The
summary below is the exact PR-body section to render. If the value is the
literal ``None``, no story was generated and the section must be omitted.

<user_story_summary>
### User Stories
- Skipped: story policy is `off` (`PDD_STORY_POLICY=off`).
</user_story_summary>

% Your Task

1. **Stage all changes**
   ```bash
   cd /tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16
   git add -A
   ```

2. **Create commit**
   - Use a clear commit message referencing the issue
   - Format: `feat: [brief description] (#16)`
   - If `git status --porcelain` shows no staged or unstaged changes after
     staging, do not create an empty commit. Still push the branch in step 3,
     then continue to the existing-PR check in step 4.

3. **Push branch**

   If `clean_restart` is `true`, the remote branch may have divergent history from the
   stopped run. Use `--force-with-lease` to overwrite it safely:
   ```bash
   git push --force-with-lease -u origin change/issue-16
   ```
   Otherwise use a standard push:
   ```bash
   git push -u origin change/issue-16
   ```

4. **Create or update pull request**

   An open PR for this branch may already exist, especially after a retry or
   resume. Always check first:
   ```bash
   gh pr list --repo aeril716/live-answer-card-test --head change/issue-16 --state open --json number,url
   ```
   - If an open PR is found, update it in place (the branch was already force-pushed):
     ```bash
     gh pr edit <number> --repo aeril716/live-answer-card-test --base main --title "feat: [description] (#16)" --body "..."
     ```
     Record that PR's number and URL as the result; skip `gh pr create`.
   - If no open PR exists, create one:
     ```bash
     gh pr create --repo aeril716/live-answer-card-test --title "feat: [description] (#16)" --body "..." --base main --head change/issue-16
     ```

   Whether creating or editing, the PR must:
   - Reference the original issue
   - Summarize the prompt changes made
   - List files modified
   - Mention generated user-story artifacts when `<user_story_summary>` is not `None`
   - Include next steps for reviewer

5. **Link PR to issue**
   - The PR body should include `Closes #16` or `Fixes #16`
   - This ensures the issue is automatically closed when PR is merged

% Output

Push the branch (use `--force-with-lease` if `clean_restart` is `true`), then create or
update the PR following the logic in step 4 above:

```bash
gh pr list --repo aeril716/live-answer-card-test --head change/issue-16 --state open --json number,url
# If no open PR exists:
gh pr create --repo aeril716/live-answer-card-test --title "feat: [description] (#16)" --body "..." --base main --head change/issue-16
# OR, if an open PR already exists:
gh pr edit <number> --repo aeril716/live-answer-card-test --base main --title "feat: [description] (#16)" --body "..."
```

After attempting to create or update the PR, return your findings in your final response.
Do not post GitHub issue comments yourself; the orchestrator extracts the
`<step_report>` block and posts it with trusted credentials when GitHub state is
enabled.

% Structured Output Artifacts (REQUIRED — source of truth)

After every PR attempt, write TWO files into the artifacts directory `/tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16/.pdd/change/issue-16` (already created for you), including failure cases where no PR was created or updated. The orchestrator reads the JSON to learn the PR URL and outcome; the PR link inside the `<step_report>` block is now a FALLBACK only.

1. `/tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16/.pdd/change/issue-16/13_create_pr.json` — the machine-readable result. Emit valid JSON:
   ```json
   {{{{
     "status": "pr_created",
     "pr_url": "https://github.com/{aeril716}/{live-answer-card-test}/pull/42",
     "pr_number": 42,
     "summary": "one-line description of the PR"
   }}}}
   ```
   - `status`: `"pr_created"` for a new PR, `"pr_updated"` when you edited an existing open PR in place (clean-restart path), or `"blocked"` when the PR could not be created/updated (the orchestrator maps this to a workflow failure).
   - `pr_url` / `pr_number`: the created/updated PR's URL and number. For `"blocked"`, set `pr_url` to `""` and omit `pr_number` if unknown.
   - `summary`: one-line PR summary, or for `"blocked"` a concise error summary naming the failed command/action.

2. `/tmp/pdd_job_pwlYgJtJT7l3U1OEcun5_pg_oucfu/.pdd/worktrees/change-issue-16/.pdd/change/issue-16/13_create_pr.md` — the exact Markdown comment body (the same content you place inside the `<step_report>` block below), posted verbatim as the `## Step 13/13` issue comment.

Still emit the `<step_report>` block below UNCHANGED — include the PR link on success, or the blocking error summary on `"blocked"`. It is the orchestrator's fallback when these files are missing.

**PR Body Format:**
```markdown
## Summary

[1-2 sentence description of what this PR does]

Closes #16

## Changes Made

### Prompts Modified
- `prompts/xxx_python.prompt` - [brief description]
- `prompts/yyy_python.prompt` - [brief description]

### Documentation Updated
- `README.md` - [brief description]

Render the contents of ``<user_story_summary>`` here. If the value is the
literal ``None``, omit this section entirely. Otherwise include it verbatim as a
PR-body section so reviewers can see which issue-derived user story was generated
and which prompt metadata it links to.

## ⚠️ Manual Review Required

Render the contents of ``<manual_review_lines>`` here. If the value is the literal ``"None"``, omit this section entirely. Otherwise emit one bullet per ``MANUAL_REVIEW`` line so reviewers see every flagged conflict in the PR body without digging through the issue thread:

- `path/to/doc.md` — [reason] — review [section]

## Review Checklist

- [ ] Prompt syntax is valid
- [ ] PDD conventions followed
- [ ] Documentation is up to date

## Next Steps After Merge

1. Regenerate code from modified prompts in dependency order:
   ```bash
   ./sync_order.sh
   ```
   Or manually:
   ```
   pdd sync audio
   ```
2. Run tests to verify functionality
3. Deploy if applicable

---
*Created by pdd change workflow*
```

**Step Report Format:**

**If PR creation/update succeeded:**
```markdown
<step_report>
## Step 13: Pull Request Created

**Status:** PR Ready for Review

### Pull Request
[PR #{{pr_number}}]({{pr_url}})

### Summary
- Branch: `change/issue-16`
- Files changed: {{N}}
- Ready for review

### What's Next
1. Review the PR
2. Run `./sync_order.sh` after merge to regenerate code in dependency order
3. Run tests to verify

---
*Agentic change workflow complete. Total cost: ${{total_cost:.4f}}*
</step_report>
```

**If PR creation/update was blocked:**
```markdown
<step_report>
## Step 13: Pull Request Blocked

**Status:** PR Creation Blocked

### Blocking Error
- Failed action: [push | create PR | update PR | detect existing PR]
- Error: [concise command/action failure summary]

### Remediation
1. [Concrete next action for the maintainer]
2. Re-run `pdd change` after the blocker is resolved

---
*Agentic change workflow blocked before PR creation. Total cost: ${{total_cost:.4f}}*
</step_report>
```

% Important

- Always link the PR to the issue using "Closes #N" or "Fixes #N"
- Push to origin before creating PR
- Include clear next steps for the reviewer
- Report the PR URL in the `<step_report>` block when a PR exists; for `"blocked"`, report the blocking error and remediation instead
- This is the final step - workflow is complete after this


You have full file access to explore and modify files as needed.