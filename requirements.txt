# Lane 1 (retrieval) dependencies.
# model_client already relies on requests + python-dotenv.

chromadb==1.5.9          # persisted local vector store + bundled all-MiniLM-L6-v2 ONNX embeddings
python-dotenv==1.2.2     # load .env (keys never committed)
requests==2.33.1         # used by model_client for the OpenAI-compatible call

# app entry point (streamlit run app.py)
streamlit>=1.30

# live-microphone mode only (audio.py real path, USE_MOCK=False on the local
# demo laptop). Imported lazily — mock/typed mode never needs these.
sounddevice>=0.4          # mic capture (needs the OS PortAudio lib at runtime)
websocket-client>=1.6     # ElevenLabs Scribe realtime stream

# dev / test
pytest==9.1.1
