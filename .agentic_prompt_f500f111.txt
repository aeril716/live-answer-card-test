% You are an expert software engineer. Your task is to create a pull request from the worktree branch and link it to the original GitHub issue.

% Context

You are working on step 13 of 13 in an agentic change workflow. This is the final step. All prompt changes have been made and verified. Now you create a PR for review.

% Inputs

- GitHub Issue URL: https://github.com/aeril716/live-answer-card-test/issues/14
- Repository: aeril716/live-answer-card-test
- Issue Number: 14
- Issue Title: [Lane 2] audio.get_utterance() — mock version
- Worktree Path: /tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14
- Artifacts Directory: /tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14/.pdd/change/issue-14
- Branch Name: change/issue-14
- Base Branch: main
- Files Changed: README.md, docs/writing-pdd-prompts.md, prompts/audio_python.prompt
- Sync Order Script: /tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/sync_order_change.sh
- Sync Order Commands: pdd sync audio
- Clean Restart Mode: false

% Issue Content
<issue_content>
Title: [Lane 2] audio.get_utterance() — mock version

Description:
## Goal

`audio.py` exposes `get_utterance()` returning one utterance at a time. Mock only — real ElevenLabs streaming comes in a later issue. This lets the whole team run the loop without a microphone.

## Contract — frozen

```python
def get_utterance() -> dict:
    return {{"text": "Are you guys SOC 2 certified?",
            "speaker": "prospect",
            "ts": 252.4}}

EMPTY = {{"text": "", "speaker": "unknown", "ts": 0.0}}
```

## Acceptance criteria

1. Given `USE_MOCK = True` at the top of the file, when `get_utterance()` is
   called, then it replays a hardcoded list and makes no network call.
2. The hardcoded list contains, in this order: a technical question about SOC 2,
   a follow-up asking Type I or Type II, a small-talk line about the weekend,
   and a pricing question. This is the demo sequence.
3. Given repeated calls, then each utterance is returned exactly once, in order.
4. Given the list is exhausted, then EMPTY is returned on every later call.
5. Given the function is called, then it returns immediately and never blocks.
6. Given any internal error, then EMPTY is returned and nothing is raised.
7. `ts` increases across the list.
8. The module prints one line per call in the form `[audio] out=<text>`.

## Must not

- Must not raise under any circumstance.
- Must not block the calling thread.
- Must not return the same utterance twice.
- Must not import websockets, requests, or any audio library in this issue.

## Validation

- Run `python audio.py`. A `__main__` block calls `get_utterance()` six times
  and prints each result, so exhaustion is visible.

## Done when

- Four utterances come out in order, then EMPTY forever
- `python audio.py` runs clean with no microphone and no network

Comments:

--- Comment by prompt-driven-github[bot] ---
🚀 **Job Queued!**

Job ID: `raJ2OCPH5mda2m7H9BNu`
Triggered by: @aeril716
Label: `pdd-change`

[View Live Progress](https://promptdriven.ai/jobs/raJ2OCPH5mda2m7H9BNu)

Budget cap: none

You can add or change a cap by commenting:
```text
/strength 0.7
/pdd budget N
/pdd settings
/pdd stop
```
_Run controls require write access; `/pdd settings` is read-only._

</issue_content>

% Change Summary (from previous steps)
<step8_output>
<step_report>
## Step 8: Prompt Change Analysis

**Status:** Changes Analyzed

### Summary
Create one concise `audio` module prompt that defines the frozen, deterministic mock utterance producer and relies only on the shared project preamble. Step 9 should also apply the previously approved README usage section and prompt-inventory documentation update; generated code, examples, and tests remain outputs of `pdd sync`.

### Prompt Modifications

#### `prompts/audio_python.prompt`
**New prompt to create:**
- Role: Create root-level `audio.py` as the dependency-free, microphone-free mock source for one utterance per call; real ElevenLabs streaming remains out of scope.
- Requirements:
  1. Preserve `get_utterance() -> dict`, the exact keys `text`, `speaker`, and `ts`, and `EMPTY = {"text": "", "speaker": "unknown", "ts": 0.0}`.
  2. With top-level `USE_MOCK = True`, return a deterministic four-item in-memory demo sequence immediately, without network, device, or audio access.
  3. Return, exactly once and in order, a technical SOC 2 certification question, a Type I-or-Type II follow-up, weekend small talk, and a pricing question; use `speaker="prospect"` and strictly increasing numeric timestamps.
  4. After the fourth item, return a fresh `EMPTY` dictionary on every later call; return fresh dictionaries so caller mutation cannot alter the sequence or sentinel.
  5. Keep calls synchronous and immediate: perform no sleep, polling, retry, input read, async wait, lock wait, background work, or external I/O other than the required stdout line.
  6. Contain every ordinary internal failure, including selection, copying, formatting, and logging failures; return a fresh `EMPTY` and expose no ordinary exception.
  7. For each ordinary call, emit exactly one line in the form `[audio] out=<text>`, including an empty suffix after exhaustion, without producing output on import.
  8. Provide a guarded `__main__` demonstration that calls `get_utterance()` exactly six times and prints each result, showing four ordered utterances followed by two empty results.
- Constraints: Must not repeat an utterance within one loaded module instance; must not block; must not import `websockets`, `requests`, audio libraries, or third-party packages; must not add a reset or configuration API.
- Dependencies: `<include>context/project_preamble.prompt</include>` only. It supplies the frozen interface, `USE_MOCK` convention, failure containment, logging, and latency rules.
- Deliverables declared by the prompt: `audio.py`, `context/audio_example.py`, and `tests/test_audio.py`.

**Extension Analysis (REQUIRED):**
- Modules considered: `retrieval`, `trigger`, `model_client`, `screen`, and `band`.
- Why not extendable: `retrieval` maps a supplied question to a card and does not produce ordered input; `trigger` classifies utterances and owns call-scoped repeat state; `model_client` is the sole provider-access boundary; `screen` only renders cards; and `band` is an optional audit publisher. Adding utterance production to any of them would mix frozen interfaces and unrelated lane responsibilities.
- Capability gap: The repository lacks a stateful, forward-only source that supplies deterministic utterance dictionaries to the future application polling loop without a microphone or network.

### Documentation Changes Carried Forward
- `README.md`: add the approved “Mock audio input” section documenting deterministic four-item replay, permanent `EMPTY`, the `[audio] out=<text>` line, and `python audio.py`.
- `docs/writing-pdd-prompts.md`: add `audio_python.prompt` to the repository prompt inventory.
- Direct Edit Candidates from Step 6: none.

### Interface Changes
- Adds the already-frozen `audio.get_utterance() -> dict` interface; no existing signature or return shape changes.
- No existing module depends on `audio` yet, so current code and tests are unaffected. New generated tests should isolate module-level sequence state by reloading the module or otherwise resetting test process state without adding a public reset API.

### Conflict Check
- The design agrees with `context/project_preamble.prompt`, the issue contract, and the later planned `app.py` polling consumer.
- The issue’s required stdout write is the only allowed I/O; “never blocks” is specified as absence of intentional waits and external/device operations rather than an absolute guarantee about stdout.
- “Nothing is raised” applies to ordinary `Exception` failures; normal interpreter-control exceptions remain outside the lane-function error contract.
- No existing tests or interfaces require migration.

### Risk Assessment
- **Low**: The module has a small frozen interface, deterministic data, no dependencies, and no integration point in the current repository.
- Mitigation: Generate tests for exact order, permanent exhaustion, strictly increasing timestamps, fresh-return isolation, one-line logging, import silence, forbidden imports, failure fallback, and the six-call script demonstration.

---
*Proceeding to Step 9: Implement Changes*
</step_report>
</step8_output>

% Manual Review Required (from orchestrator aggregation)

The orchestrator collected every ``MANUAL_REVIEW: <path> — <reason>`` line emitted by Step 9 JSON/prose, synthesized from Step 10 associated-doc conflicts, or emitted by Step 11/12 review-loop JSON/prose. These are items the workflow could not safely auto-resolve. They MUST be surfaced in the PR body so reviewers can address them — not buried elsewhere on the issue thread.

<manual_review_lines>
None
</manual_review_lines>

% User Story Coverage (from issue-derived story generation)

The orchestrator generated an issue-derived user story for the existing,
non-runtime prompts changed by this workflow and staged it for this PR. The
summary below is the exact PR-body section to render. If the value is the
literal ``None``, no story was generated and the section must be omitted.

<user_story_summary>
### User Stories
- Skipped: story policy is `off` (`PDD_STORY_POLICY=off`).
</user_story_summary>

% Your Task

1. **Stage all changes**
   ```bash
   cd /tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14
   git add -A
   ```

2. **Create commit**
   - Use a clear commit message referencing the issue
   - Format: `feat: [brief description] (#14)`
   - If `git status --porcelain` shows no staged or unstaged changes after
     staging, do not create an empty commit. Still push the branch in step 3,
     then continue to the existing-PR check in step 4.

3. **Push branch**

   If `clean_restart` is `true`, the remote branch may have divergent history from the
   stopped run. Use `--force-with-lease` to overwrite it safely:
   ```bash
   git push --force-with-lease -u origin change/issue-14
   ```
   Otherwise use a standard push:
   ```bash
   git push -u origin change/issue-14
   ```

4. **Create or update pull request**

   An open PR for this branch may already exist, especially after a retry or
   resume. Always check first:
   ```bash
   gh pr list --repo aeril716/live-answer-card-test --head change/issue-14 --state open --json number,url
   ```
   - If an open PR is found, update it in place (the branch was already force-pushed):
     ```bash
     gh pr edit <number> --repo aeril716/live-answer-card-test --base main --title "feat: [description] (#14)" --body "..."
     ```
     Record that PR's number and URL as the result; skip `gh pr create`.
   - If no open PR exists, create one:
     ```bash
     gh pr create --repo aeril716/live-answer-card-test --title "feat: [description] (#14)" --body "..." --base main --head change/issue-14
     ```

   Whether creating or editing, the PR must:
   - Reference the original issue
   - Summarize the prompt changes made
   - List files modified
   - Mention generated user-story artifacts when `<user_story_summary>` is not `None`
   - Include next steps for reviewer

5. **Link PR to issue**
   - The PR body should include `Closes #14` or `Fixes #14`
   - This ensures the issue is automatically closed when PR is merged

% Output

Push the branch (use `--force-with-lease` if `clean_restart` is `true`), then create or
update the PR following the logic in step 4 above:

```bash
gh pr list --repo aeril716/live-answer-card-test --head change/issue-14 --state open --json number,url
# If no open PR exists:
gh pr create --repo aeril716/live-answer-card-test --title "feat: [description] (#14)" --body "..." --base main --head change/issue-14
# OR, if an open PR already exists:
gh pr edit <number> --repo aeril716/live-answer-card-test --base main --title "feat: [description] (#14)" --body "..."
```

After attempting to create or update the PR, return your findings in your final response.
Do not post GitHub issue comments yourself; the orchestrator extracts the
`<step_report>` block and posts it with trusted credentials when GitHub state is
enabled.

% Structured Output Artifacts (REQUIRED — source of truth)

After every PR attempt, write TWO files into the artifacts directory `/tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14/.pdd/change/issue-14` (already created for you), including failure cases where no PR was created or updated. The orchestrator reads the JSON to learn the PR URL and outcome; the PR link inside the `<step_report>` block is now a FALLBACK only.

1. `/tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14/.pdd/change/issue-14/13_create_pr.json` — the machine-readable result. Emit valid JSON:
   ```json
   {{{{
     "status": "pr_created",
     "pr_url": "https://github.com/{aeril716}/{live-answer-card-test}/pull/42",
     "pr_number": 42,
     "summary": "one-line description of the PR"
   }}}}
   ```
   - `status`: `"pr_created"` for a new PR, `"pr_updated"` when you edited an existing open PR in place (clean-restart path), or `"blocked"` when the PR could not be created/updated (the orchestrator maps this to a workflow failure).
   - `pr_url` / `pr_number`: the created/updated PR's URL and number. For `"blocked"`, set `pr_url` to `""` and omit `pr_number` if unknown.
   - `summary`: one-line PR summary, or for `"blocked"` a concise error summary naming the failed command/action.

2. `/tmp/pdd_job_raJ2OCPH5mda2m7H9BNu_1eath3a6/.pdd/worktrees/change-issue-14/.pdd/change/issue-14/13_create_pr.md` — the exact Markdown comment body (the same content you place inside the `<step_report>` block below), posted verbatim as the `## Step 13/13` issue comment.

Still emit the `<step_report>` block below UNCHANGED — include the PR link on success, or the blocking error summary on `"blocked"`. It is the orchestrator's fallback when these files are missing.

**PR Body Format:**
```markdown
## Summary

[1-2 sentence description of what this PR does]

Closes #14

## Changes Made

### Prompts Modified
- `prompts/xxx_python.prompt` - [brief description]
- `prompts/yyy_python.prompt` - [brief description]

### Documentation Updated
- `README.md` - [brief description]

Render the contents of ``<user_story_summary>`` here. If the value is the
literal ``None``, omit this section entirely. Otherwise include it verbatim as a
PR-body section so reviewers can see which issue-derived user story was generated
and which prompt metadata it links to.

## ⚠️ Manual Review Required

Render the contents of ``<manual_review_lines>`` here. If the value is the literal ``"None"``, omit this section entirely. Otherwise emit one bullet per ``MANUAL_REVIEW`` line so reviewers see every flagged conflict in the PR body without digging through the issue thread:

- `path/to/doc.md` — [reason] — review [section]

## Review Checklist

- [ ] Prompt syntax is valid
- [ ] PDD conventions followed
- [ ] Documentation is up to date

## Next Steps After Merge

1. Regenerate code from modified prompts in dependency order:
   ```bash
   ./sync_order.sh
   ```
   Or manually:
   ```
   pdd sync audio
   ```
2. Run tests to verify functionality
3. Deploy if applicable

---
*Created by pdd change workflow*
```

**Step Report Format:**

**If PR creation/update succeeded:**
```markdown
<step_report>
## Step 13: Pull Request Created

**Status:** PR Ready for Review

### Pull Request
[PR #{{pr_number}}]({{pr_url}})

### Summary
- Branch: `change/issue-14`
- Files changed: {{N}}
- Ready for review

### What's Next
1. Review the PR
2. Run `./sync_order.sh` after merge to regenerate code in dependency order
3. Run tests to verify

---
*Agentic change workflow complete. Total cost: ${{total_cost:.4f}}*
</step_report>
```

**If PR creation/update was blocked:**
```markdown
<step_report>
## Step 13: Pull Request Blocked

**Status:** PR Creation Blocked

### Blocking Error
- Failed action: [push | create PR | update PR | detect existing PR]
- Error: [concise command/action failure summary]

### Remediation
1. [Concrete next action for the maintainer]
2. Re-run `pdd change` after the blocker is resolved

---
*Agentic change workflow blocked before PR creation. Total cost: ${{total_cost:.4f}}*
</step_report>
```

% Important

- Always link the PR to the issue using "Closes #N" or "Fixes #N"
- Push to origin before creating PR
- Include clear next steps for the reviewer
- Report the PR URL in the `<step_report>` block when a PR exists; for `"blocked"`, report the blocking error and remediation instead
- This is the final step - workflow is complete after this


You have full file access to explore and modify files as needed.